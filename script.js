const nav=document.getElementById('nav');const menuBtn=document.getElementById('menuBtn');const mobileMenu=document.getElementById('mobileMenu');const cartBtn=document.getElementById('cartBtn');const closeCart=document.getElementById('closeCart');const cartDrawer=document.getElementById('cartDrawer');const overlay=document.getElementById('overlay');const cartItems=document.getElementById('cartItems');const cartCount=document.getElementById('cartCount');const cartTotal=document.getElementById('cartTotal');const langBtn=document.getElementById('langBtn');document.getElementById('year').textContent=new Date().getFullYear();
const cart=[];
function renderCart(){if(!cart.length){cartItems.innerHTML='<div class="empty-cart">السلة فارغة<br><span>أضف منتجًا لتبدأ</span></div>';cartCount.textContent='0';cartTotal.textContent='0 ر.س';return}cartItems.innerHTML=cart.map((p,i)=>`<div class="cart-row"><div><strong>${p.name}</strong><small>${p.price} ر.س</small></div><button class="cart-remove" data-index="${i}">إزالة</button></div>`).join('');cartCount.textContent=cart.length;cartTotal.textContent=`${cart.reduce((s,p)=>s+p.price,0)} ر.س`;document.querySelectorAll('.cart-remove').forEach(b=>b.addEventListener('click',()=>{cart.splice(+b.dataset.index,1);renderCart()}))}
function openCart(){cartDrawer.classList.add('open');overlay.classList.add('open');document.body.style.overflow='hidden'}function hideCart(){cartDrawer.classList.remove('open');overlay.classList.remove('open');document.body.style.overflow=''}
menuBtn?.addEventListener('click',()=>mobileMenu.classList.toggle('open'));mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileMenu.classList.remove('open')));cartBtn?.addEventListener('click',openCart);closeCart?.addEventListener('click',hideCart);overlay?.addEventListener('click',hideCart);
document.querySelectorAll('.add-demo').forEach(btn=>btn.addEventListener('click',()=>{cart.push({name:btn.dataset.product,price:Number(btn.dataset.price)});renderCart();openCart()}));
window.addEventListener('scroll',()=>{nav.classList.toggle('scrolled',window.scrollY>30)});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
window.addEventListener('mousemove',e=>{document.querySelectorAll('.parallax').forEach(el=>{const r=el.getBoundingClientRect();const x=(e.clientX-(r.left+r.width/2))*0.015;const y=(e.clientY-(r.top+r.height/2))*0.01;el.style.transform=`translate(${x}px,${y}px)`})});
let english=false;langBtn?.addEventListener('click',()=>{english=!english;document.documentElement.dir=english?'ltr':'rtl';document.documentElement.lang=english?'en':'ar';langBtn.textContent=english?'AR':'EN';const map=english?{'.hero h1':'Tech<br><em>that moves</em> with you','.hero-text':'Mobile accessories built around performance, design and reliability — from the first charge to the last moment of your day.','.hero-actions .btn':'Explore products ↗','.eyebrow':'ROCK / EVERYDAY TECH'}:{};if(english){document.querySelector('.hero h1').innerHTML=map['.hero h1'];document.querySelector('.hero-text').textContent=map['.hero-text'];document.querySelector('.hero-actions .btn').innerHTML=map['.hero-actions .btn'];document.querySelectorAll('.eyebrow')[0].textContent=map['.eyebrow']}else{location.reload()}});
renderCart();